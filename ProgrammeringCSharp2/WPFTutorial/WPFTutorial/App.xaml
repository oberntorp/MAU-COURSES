<Application x:Class="WPFTutorial.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:WPFTutorial"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <LinearGradientBrush x:Key="Background">
            <GradientStop Offset="0" Color="Silver"/>
            <GradientStop Offset="1" Color="LightGreen" />
            <GradientStop Offset="2" Color="Silver" />
        </LinearGradientBrush>
        <LinearGradientBrush x:Key="BtnsBackground">
            <GradientStop Offset="0" Color="White"/>
            <GradientStop Offset="1" Color="Blue" />
        </LinearGradientBrush>

        <Style TargetType="{x:Type Button}">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="5, 2"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="BlueViolet"/>
            <Setter Property="Background" Value="{StaticResource BtnsBackground}" />
            <Setter Property="Width" Value="250"/>
        </Style>
    </Application.Resources>
</Application>
